import { h, ComponentChildren } from 'preact';
import Row from '../../row';
export declare function TR(props: {
    row?: Row;
    messageRow?: boolean;
    children?: ComponentChildren;
}): h.JSX.Element;
